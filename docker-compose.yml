version: "3.3"  # optional since v1.27.0
services:
  mysql:
    image: "mysql:8.0"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
      MYSQL_DATABASE: 'bluebank'
    ports:
      - "3306:3306"